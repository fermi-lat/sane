package sane

version v1r3p1

author T. Burnett <tburnett@u.washington.edu>
# $Id$

use STpolicy *
use dataSubselector * -no_auto_imports
use Likelihood * -no_auto_imports
use observationSim * -no_auto_imports
use map_tools * -no_auto_imports
use python * IExternal -no_auto_imports
use st_app *

application test_sane test/main.cxx

path_append PYTHONPATH "$(SANEROOT)/python"

#any package using this one will make a local executable

pattern -global user_package \
   application <package> "$(SANEROOT)/src/sane_main.cxx $(sane_extra)" 


private
#------------------------------------------------------------------------


macro sane_extra "" WIN32 " -s=..\python *.py "


